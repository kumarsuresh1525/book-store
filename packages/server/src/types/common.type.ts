type Title = {
  title: string;
};

type Author = {
  author: string;
};

type Price = {
  price: number;
};

type Rating = {
  rating: number;
};

export type TSearch = Title | Author | Price | Rating;
